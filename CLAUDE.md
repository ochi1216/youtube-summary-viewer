# CLAUDE.md

このリポジトリで Claude Code が作業する際のルールを定義する。

プロジェクト名: **(H)Podcast オーガナイザー**

---

## 0. このリポジトリの位置づけ

本リポジトリは **公開用ビューア（`index.html`）の配信専用**である。

要約パイプライン本体（YouTube / RSS / Podcast の収集・要約スクリプトと統合スクリプト）は
家PC側にあり、**このリポジトリでは管理していない**。詳細は `docs/PROJECT_STATUS.md` を参照。

---

## 1. 変更禁止事項

- **`index.html` を手で編集しない。**
  これは家PC側の `consolidated_html_summary_manager.py` が生成する成果物であり、
  手編集しても次回生成時に上書きされて失われる。
  ビューアの表示・挙動を変えたい場合は、生成側スクリプトを修正すること。
- 既存ファイルを削除・移動・リネームしない。
- Git履歴を書き換えない（merge / rebase / reset を勝手に実行しない）。
- APIキー、パスワード、認証情報を、リポジトリ内のいかなるファイルにも記載しない。
  管理ファイルに書いてよいのは**環境変数名までで、値は書かない**。

---

## 2. セッション管理

### セッションの定義

- 1セッションとは、Claude Code Web 上で新しく作成された1つの会話セッションを指す。
- 「1つの明確な目的＝1セッション」を基本とする。
- 同じセッション内の追加修正・表示調整・テスト・再修正は、
  **現在のセッション内の作業項目**として扱う。
- 同じセッション内ではセッション番号を変更しない。
- 新しい Claude Code Web セッションを作成した場合だけ、セッション番号を1つ増やす。

### 同一セッション内の運用

- 細かな追加依頼ごとに `docs/SESSION_HISTORY.md` を更新しない。
- 細かな追加依頼ごとに `docs/NEXT_TASK.md` を更新しない。
- 細かな追加依頼ごとにコミットや Push を行わない。
- 軽微な修正や試行錯誤の途中では、原則としてコミット・Push を行わない。
- 追加作業の完了時は、**変更内容とテスト結果だけを簡潔に報告**する。

### セッション終了

セッション終了処理は、ユーザーが以下のいずれかを明示した場合**だけ**実施する。

- このセッションを終了する
- 引継ぎを作成する
- 次のセッションへ移る
- セッション終了処理を行う

終了時にまとめて以下を実施する。

1. 必要に応じて `docs/PROJECT_STATUS.md` を更新する
2. `docs/SESSION_HISTORY.md` に今回のセッションを **1件だけ** 追記する
3. `docs/NEXT_TASK.md` を次回用に更新する
4. 差分とテスト結果を確認する
5. 必要な変更をコミットする
6. リモートブランチへ Push する
7. 未コミット変更と未追跡ファイルを確認する
8. 次回タイトルと開始プロンプトを提示する

---

## 3. 管理ファイル

| ファイル | 役割 |
|---|---|
| `CLAUDE.md`（本ファイル） | 作業ルール・セッション管理ルール |
| `docs/PROJECT_STATUS.md` | プロジェクトの現状（確認できた事実のみ） |
| `docs/SESSION_HISTORY.md` | セッション単位の作業履歴 |
| `docs/NEXT_TASK.md` | 次セッションで着手する内容 |

`docs/PROJECT_STATUS.md` には、**確認できない内容は「未確認」と書く**。
推測で機能や仕様を追加しない。

---

## 4. 公開フロー

`main` ブランチへ push すると、GitHub Actions（`.github/workflows/pages.yml`）が
GitHub Pages へ自動デプロイする。ページには `noindex, nofollow` が設定されている。

作業ブランチから直接 `main` へ push しない。
